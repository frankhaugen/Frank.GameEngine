# Ensures build + TUnit (Microsoft.Testing.Platform) pass on .NET 10 SDK (linux + windows).
# Reusable workflows under frankhaugen/Workflows may use different commands; this job is the source of truth for this repo.
name: Verify .NET

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Restore
        run: dotnet restore Frank.GameEngine.slnx

      # Let `dotnet test` build test targets so outputs line up with MTP discovery (avoids stale/no-match binaries vs `--no-build`).
      - name: Test (Microsoft.Testing.Platform)
        run: dotnet test --solution Frank.GameEngine.slnx -c Release --no-restore
